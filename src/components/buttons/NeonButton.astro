---
export interface Props {
    href?   : string;
    id?     : string;
}

const { href, id } = Astro.props;
---

<a
    href    = { href }
    id      = { id ?? '' }
    class   = "neon_button hover:scale-105 rounded-lg font-semibold flex items-center gap-2 justify-center"
>
    <slot/>
</a>
